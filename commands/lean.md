---
description: Switch this session into maximum token-efficiency mode
---

Load and follow the `token-efficiency` skill for the rest of this session:
read less (targeted reads, terse flags, scout subagent for exploration),
write less (minimal-code decision ladder), say less (answer-first, no
narration). Confirm in one line that lean mode is active, then continue
with $ARGUMENTS if provided.
